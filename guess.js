"use strict"; 

 alert("Welcome"); 

 let askName = prompt("Can I get your name?");  

 console.log(askName);  

 alert("Hey" + askName) 

  // give user a hint  

let response = prompt("Are you ready to learn more about me?") 

 if (response.toLowerCase() === 'y' || response.toLowerCase() === 'yes') 

    { 
     alert("awesome") 
    }  

 let askUser = prompt("Am I 19?");  

{ 
    let response = prompt("Am I 19?") 

    if (response.toLowerCase() === 'n' || response.toLowerCase() === 'no');  
    {
        alert('great');
    }
} 

let youSure = prompt("Are you sure?")
{
    let response = prompt("Are you sure?")

    if (response.toLowerCase() === 'y' || response.toLowerCase() === 'yes');
}

let car = prompt("Do I have a Nissan?")
{
    let response = prompt("Do I have a Nissan?")

    if (response.toLowerCase() === 'y' || response.toLowerCase() === 'yes');
}

let endGoal = prompt('Do I want to create an app?')
{
    let response = prompt('Do I want to create an app?')

    if (response.toLowerCase() === 'y' || response.toLowerCase() === 'yes');
}

let number = prompt('Im thinking of a number can you guess it?')
{
    let response = prompt('"Im thinking of a number, can you guess it?')

    if (response  > 5)
    alert("Too high")

    else if (reponse < 2)
    alert("Too low");
}

let answer = prompt("How old are you?");
console.log(answer);

let age = ['19', '20', '21', '35', '40'];

for (let i = 0; i < age.length; i++) {
   console.log(age[i]); 
}

